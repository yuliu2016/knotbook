package kb.service.api.array;

import java.util.List;

public final class BaseTableArray extends TableArray {
    public BaseTableArray(int cols, int len, byte[] mode, float[] num, List<String> str) {
        super(cols, len, mode, num, str);
    }
}
